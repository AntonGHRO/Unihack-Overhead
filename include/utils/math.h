#ifndef OH_UTILS_MATH_H
#define OH_UTILS_MATH_H

#include <stdint.h>

// ---------------------------------------------------------------------
// Returns the next power of 2 found after the given input
uint64_t oh_utils_math_nextpow2(uint64_t x);

#endif
